{
  "name": "@cernio/types",
  "version": "0.1.0",
  "description": "Shared TypeScript types for Cernio",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
