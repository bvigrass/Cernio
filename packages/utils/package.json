{
  "name": "@cernio/utils",
  "version": "0.1.0",
  "description": "Shared utility functions for Cernio",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "echo \"No tests for utils package\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
